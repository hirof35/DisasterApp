<Window x:Class="DisasterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DisasterApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        xmlns:tb="http://www.hardcodet.net/taskbar">
    <Grid x:Name="CaptureArea" Background="#1E1E1E">
            <StackPanel Margin="20">
                <TextBlock Text="🚨 防災備蓄管理" FontSize="28" Foreground="Yellow" FontWeight="Bold" Margin="0,0,0,10"/>

            <Border Background="#2D2D2D" CornerRadius="5" Padding="10" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="新規追加" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="NewItemName" Grid.Column="0" Margin="2" Padding="3" ToolTip="品名"/>

                        <TextBox x:Name="NewItemCurrentQty" Grid.Column="1" Margin="2" Padding="3" Text="0" HorizontalContentAlignment="Center"/>

                        <TextBox x:Name="NewItemTargetQty" Grid.Column="2" Margin="2" Padding="3" Text="3" HorizontalContentAlignment="Center"/>

                        <Button Content="追加" Click="AddItem_Click" Grid.Column="3" 
                    Background="SteelBlue" Foreground="White" Margin="2" FontWeight="Bold"/>
                    </Grid>

                    <Grid Margin="2,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="65"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="[品名]" Foreground="Gray" FontSize="10" Grid.Column="0"/>
                        <TextBlock Text="[今ある数]" Foreground="Gray" FontSize="10" Grid.Column="1" HorizontalAlignment="Center"/>
                        <TextBlock Text="[目標数]" Foreground="Gray" FontSize="10" Grid.Column="2" HorizontalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <ListView x:Name="StockList" Background="Transparent" BorderThickness="0" Height="450">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#3F3F3F" BorderThickness="0,0,0,1" Padding="10">
                                <Grid Width="420">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding ExpiryDate, StringFormat='期限: {0:yyyy/MM/dd}'}" FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="LightGray"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExpiringSoon}" Value="True">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ProgressBar Value="{Binding CurrentQty}" Maximum="{Binding TargetQty}" Height="12" Margin="0,8"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="－" Click="Minus_Click" Width="30" Height="30" Margin="2,0"/>
                                        <Button Content="＋" Click="Plus_Click" Width="30" Height="30" Margin="2,0"/>
                                        <Button Content="削除" Click="Delete_Click" Width="40" Height="30" 
                                            Background="#555" Foreground="White" FontSize="10" Margin="5,0"/>
                                        <Button Content="Amazon" Click="SearchAmazon_Click" Background="Orange" Padding="5" Margin="2,0,0,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSufficient}" Value="False">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button Content="📸 防災カードを保存(画像)" Click="ExportImage_Click" 
                    Margin="0,10,0,0" Height="45" Background="#444" Foreground="White"/>
            </StackPanel>
        <tb:TaskbarIcon x:Name="MyNotifyIcon" 
                
                ToolTipText="防災備蓄管理ガイド">
            
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                    <MenuItem Header="開く" Click="MenuOpen_Click" />
                    <MenuItem Header="終了" Click="MenuExit_Click" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </Grid>
    </Window>
